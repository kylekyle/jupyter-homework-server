# Jupyter Homework Server

This server grades Jupyter notebooks.

# Installation

This project requires Ruby 2.4. Use `bundler` to install dependencies and run:

```bash
gem install bundler
bundle install
bundle exec rackup
```
